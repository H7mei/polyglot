# Readline / Getting Input From User

nama <- readline(prompt = "Masukkan nama: ")

# print(paste("Halo", nama)) # paste() is like concat() in PHP

# parsing / casting / convert

# as.integer() / as.int() / as.numeric() / as.num()
# as.double() / as.numeric() / as.num()
# as.character() / as.char()
# as.logical() / as.bool()
