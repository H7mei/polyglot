/* 

Font Strategies


*/

// font-display: block;
// temporary renders an invisible font until the custom font has been downloaded.

// font-display: swap;
// use the custom font if it's available, use a fallback font if the custom font is not available.

// font-display: fallback;
// use a fallback font white the custom font is being downloaded. swap to the custom font when it's available.

// font-display: optional;
// only use the custom font if it's available, and use a fallback font if the custom font is not available.

// font-display: auto;
// allows the browser to determine the most apprrpriate behavior for font loading.
